# Set up project
1. Install [docker](https://docs.docker.com/install/)
2. Install [docker-compose](https://docs.docker.com/compose/install/)
3. Create folder for database data
Create a folder named `data` in `db/`


# Run project
Run `docker-compose up -d`
Navigate to `localhost:8080`

# Output
1. Query to get name of youngest customers is in `db/scripts/`
2. [Postman collection](https://www.getpostman.com/collections/1d05daee923a41ca7246)